
#ifndef NODE_H
#define NODE_H
/*

class Node  
{

public:
	Node();
	Node(int);
	~Node();
	void AddNode(int); // Params: Weight
	void SetWeight(int); // Params: Number, Weight
//	void DeleteNode (Node*); // Params: Node pointer
	void AddLink (Node*,Node*); // Params: Parent node pointer, child node pointer
//	void DeleteLink (Node*, Node*); //Params: Parent node pointer, child node pointer
///	void Analyze (Node*);

    static int getCount(){return count; }
	int getWeight ()const;
	
private:
	
	int * pParents, // Parents pointers ( = 0 for index node)
		* pChilds;	 // Childrens pointers ( = 0 for Last node)
	int Weight,
		Number;
	bool isReady;

	static int count;// Count of Nodes
	int *hash;

//	void DrawNode (int,int); // Draw node circle. Params: X,Y
	void AddToHash();
};*/
#endif
